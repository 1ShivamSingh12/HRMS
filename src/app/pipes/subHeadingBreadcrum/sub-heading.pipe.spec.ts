import { SubHeadingPipe } from './sub-heading.pipe';

describe('SubHeadingPipe', () => {
  it('create an instance', () => {
    const pipe = new SubHeadingPipe();
    expect(pipe).toBeTruthy();
  });
});
